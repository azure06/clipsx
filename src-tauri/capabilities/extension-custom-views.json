{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "extension-custom-views",
  "description": "Extension child views may call only the authenticated ClipsX extension bridge.",
  "local": true,
  "webviews": ["extension-*"],
  "permissions": ["allow-extension-bridge"]
}
