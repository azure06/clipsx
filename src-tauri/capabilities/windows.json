{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "windows-capability",
  "description": "Windows-only window chrome permissions",
  "platforms": ["windows"],
  "windows": ["main"],
  "permissions": ["decorum:allow-show-snap-overlay"]
}
